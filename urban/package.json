{
  "name": "my-simple-web-app",
  "version": "1.0.0",
  "description": "A basic application to test the build process.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "express": "^4.19.2"
  }
}
